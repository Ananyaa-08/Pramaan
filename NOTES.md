PYTHONPATH quirk on this machine: editable install .pth registers correctly but site.addsitedir() does not process it — root cause not fully isolated (likely pyenv-built interpreter site-module behavior). Workaround: PYTHONPATH=src required for ad-hoc scripts outside pytest.

Deferred HTTPX retry policy (pre-Phase-2 tracking note): `connectors/retry.py` currently classifies built-in `ConnectionError`, `TimeoutError`, and `OSError`. Real GitHub failures arrive through `httpx`, including `httpx.ConnectError` and `httpx.TimeoutException`. HTTP 429 and HTTP 5xx also need a deliberate response policy, including how to handle `Retry-After`. This must be resolved before Phase 9 hardening or serious live GitHub connector use. It is deliberately not being fixed in this correction batch.
